/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Di sini Anda bisa menambahkan konfigurasi lain nanti
  // misalnya:
  // images: {
  //   domains: ['contoh.com'],
  // },
};

module.exports = nextConfig;